{
  "name": "backend",
  "version": "1.0.0",
  "main": "server.js",
  "scripts": {
    "i": "npm run install:all",
    "py": "pip install -r requirements.txt",
    "install:all": "npm install && pip install -r requirements.txt",
    "start": "node server.js && uvicorn main:app --reload --host 0.0.0.0 --port 8000",
    "dev": "uvicorn main:app --reload --host 0.0.0.0 --port 8000 && nodemon server.js",
    "server": "nodemon server.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "axios": "^1.9.0",
    "bcryptjs": "^2.4.3",
    "cheerio": "^1.0.0",
    "cookie-parser": "^1.4.7",
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.18.3",
    "http-proxy-middleware": "^2.0.6",
    "jsonwebtoken": "^9.0.2",
    "mongoose": "^8.2.1",
    "validator": "^13.11.0"
  },
  "devDependencies": {
    "nodemon": "^3.1.0"
  }
}
